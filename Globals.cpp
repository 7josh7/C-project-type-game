#include "Globals.h"

// Global constants for screen dimensions and stage target scores
int SCREEN_HEIGHT = 25;
int SCREEN_WIDTH = 80;
int STAGE_1_TARGET_SCORE = 20;
int STAGE_2_TARGET_SCORE = 20;
int STAGE_3_TARGET_SCORE = 20;
int STAGE_4_TARGET_SCORE = 20;

// Global variable to keep track of the score across different stages
int globalScore = 0;

